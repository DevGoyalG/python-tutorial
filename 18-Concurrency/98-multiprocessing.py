# multiprocessing in python - a technique that allows a single process to spawn and run multiple threads concurrently.
# A thread is the smallest unit of execution inside a program.
# a technique that allows a single process to spawn and run multiple threads concurrently. 
# A thread is the smallest unit of execution inside a program. In Python, multithreading allows a program to split a main task 
# into several smaller sub-tasks that run in an overlapping manner, sharing the same memory space, code, and variables


